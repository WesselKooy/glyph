# Glyph Web

Next.js App Router frontend for the Glyph Link Grid puzzle experience.

## Development

From the repository root:

```sh
pnpm dev
```

Or run the web workspace directly:

```sh
pnpm --filter @glyph/web dev
```
